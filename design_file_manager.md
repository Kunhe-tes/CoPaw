该界面是一个覆盖在业务页面上的**半屏文件管理浮层**，采用“顶部主工具栏 + 快捷目录条 + 三栏连续目录浏览”的结构。三栏不是三个固定模块，而是一个随目录深入不断向左滚动的窗口：

> 始终展示当前路径中连续的三个层级；进入下一层后，最左一层退出，新增下一层显示在最右侧。

用户打开文件管理器后，默认进入**主工作目录**，不需要再次选择入口目录。

---

# 一、整体布局

浮层居中显示在原业务页面上方，背景增加深色半透明遮罩并禁止操作。浮层使用白色背景、圆角和轻阴影，宽度约占视口的四分之三，高度约占视口的 85%。

从上到下分为三个区域：

1. 顶部主工具栏
2. 快捷目录导航条
3. 三栏文件浏览区

不设置底部状态栏。

---

# 二、顶部主工具栏

顶部左侧为面包屑路径，例如：

```text
Home > Projects > Acme Web App > docs
```

点击任意路径层级，可以直接跳转到对应目录。当前目录使用蓝色强调。

顶部右侧包含：

* 当前目录搜索框
* Upload 上传文件按钮
* 关闭浮层按钮

其中，Upload 是“上传文件”的操作按钮，与快捷目录条中的“上传目录”跳转入口语义不同。

---

# 三、快捷目录条

主工具栏下方设置独立的快捷目录导航条，包含：

* 上传目录
* 下载目录
* 工作目录
* 回收站
* 对话目录

图标语义应保持一致：

* 上传目录：向上箭头
* 下载目录：向下箭头
* 工作目录：文件夹
* 回收站：垃圾桶
* 对话目录：对话气泡

点击快捷目录后，左栏展示该快捷目录本身作为路径锚点；目录的直接子项从中栏开始展示，首个文件夹的内容展示在右栏。当前快捷目录使用浅蓝色背景、蓝色图标和蓝色文字表示激活状态。

用户首次打开浮层时，默认激活并进入**工作目录**。

---

# 四、核心三栏交互逻辑

三栏采用 Miller Columns 的连续目录浏览方式，展示路径中相邻的三个层级。

假设目录结构为：

```text
A → B → C → D
```

切换快捷目录后的初始界面展示：

```text
快捷目录锚点 | A 的内容（选中 B） | B 的内容
```

当用户点击右栏中的文件夹 C 后，界面整体向左滚动，重新展示：

```text
B | C | D
```

即：

* 原来的第一栏 A 被移出。
* 原来的第二栏 B 移到第一栏。
* 原来的第三栏 C 移到第二栏。
* C 的下一级目录 D 出现在第三栏。

继续点击 D 时，再变为：

```text
C | D | E
```

因此，三栏始终是当前目录路径上的**连续三个层级窗口**，而不是固定的“父目录栏、当前目录栏、预览栏”。

点击文件夹时，依据来源栏位转换窗口：

* 点击左栏：左栏补入原左栏目录的父目录（快捷目录根没有父目录时补入快捷目录锚点），原左栏移到中栏，被点击文件夹的内容显示在右栏。
* 点击中栏：左栏和中栏保持不动，被点击文件夹的内容显示在右栏。
* 点击右栏：原中栏移到左栏，原右栏移到中栏，被点击文件夹的内容显示在右栏。

---

# 五、目录与文件的展示规则

每一栏均采用相同的简洁列表样式，不使用传统表格：

* 不展示 Size 列。
* 不展示 Modified 列。
* 不展示表头。
* 每行显示图标、名称和一行弱化的日期信息。
* 文件名过长时使用省略号。

文件夹使用蓝色文件夹图标，Markdown、图片、PDF 等文件使用对应类型图标。

当前选中的目录或文件使用浅蓝色背景高亮。

---

# 六、最右栏的动态行为

最右栏根据当前选中对象动态变化。

## 选中文件夹

当用户选中文件夹时，最右栏展示该文件夹的直接子项。

例如第二栏当前为 B，用户选中其中的文件夹 C，第三栏立即展示 C 内部的目录和文件。用户点击进入 C 后，三栏从：

```text
A | B | C
```

切换为：

```text
B | C | D
```

## 选中文件

当用户选中文件时，最右栏不再展示下一层目录，而是展示文件预览。

预览区域包括：

* 文件类型图标
* 文件名
* 文件大小
* Preview / Details 标签
* Edit 编辑按钮
* 文件正文或通用文件信息

支持的文件可直接预览；不支持的文件类型显示基本信息和下载入口。

---

# 七、文件操作

## 编辑

文件被选中后，最右栏顶部显示 Edit 按钮。点击后，右侧预览区域原位切换为编辑模式，提供保存和取消操作。

## 删除

文件被选中后，其所在行右侧显示删除图标，未选中的文件不显示。

点击删除后弹出确认框。普通目录中的删除默认是移动到回收站，而不是永久删除。删除完成后：

* 当前文件从列表中移除。
* 右侧预览清空。
* 显示“已移至回收站”的轻量提示。

在回收站中，文件操作改为“恢复”和“永久删除”。

## 上传

点击顶部 Upload 按钮打开文件选择器。上传完成后刷新当前目录，并自动选中新上传的文件。

---

# 八、滚动与联动

三个栏位分别独立纵向滚动，不允许某一栏滚动时带动整个浮层。

任何目录切换都必须同步更新：

* 面包屑路径
* 快捷目录激活状态
* 三栏目录内容
* 当前选中项
* 文件预览状态

进入新目录时，不应默认选中第一个文件；最右栏应显示空状态，直到用户主动选择目录或文件。

---

# 九、关键空状态

没有选中内容时，最右栏显示：

```text
选择文件或文件夹以查看内容
```

空目录显示：

```text
此目录为空
```

目录加载失败时，仅在对应栏位显示错误和重新加载按钮，不关闭整个文件管理器。

---

# 十、开发验收重点

1. 用户打开浮层后默认进入主工作目录。
2. 快捷目录条独立位于顶部主工具栏下方。
3. 三栏展示路径中连续的三个目录层级。
4. `A | B | C` 点击 C 后必须切换为 `B | C | D`。
5. 选中文件夹时，最右栏展示下一层目录。
6. 选中文件时，最右栏切换为文件预览。
7. 三栏均使用统一列表样式，不展示 Size 和 Modified 列。
8. 文件选中后才显示删除按钮。
9. 编辑按钮固定在文件预览区顶部。
10. 面包屑、快捷目录和三栏内容必须保持同步。
