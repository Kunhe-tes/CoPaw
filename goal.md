## 上下文引用与提交

- 新增统一的“上下文引用”模型，支持：
  - 技能
  - MCP 工具
  - 工作区文件
- 引用只作用于下一条消息，发送后清空。
- 选中项按稳定标识去重；跨类型同名项可同时存在。
- 后端接收结构化引用，不依赖输入文本解析。
- 按类型注入受信任上下文：
  - 技能：明确选择使用。
  - MCP 工具：优先但不强制调用。
  - 文件：后端重新校验后提示 Agent 按需读取，不注入文件内容。
- 文件引用只能指向当前 Agent 工作区的 `media` 或 `static`，并在提交时再次校验。

## @ 浮层与选择交互

- 输入 `@` 时，仅展示当前技能和当前可调用 MCP 工具。
- `@` 后输入搜索词时，防抖约 200 ms 后搜索技能、MCP 工具和文件。
- 文件只按文件名匹配；`media` 与 `static` 合并在“文件”分组。
- 每个类别最多展示 4 项。
- 分组固定顺序：技能 → MCP 工具 → 文件。
- 没有结果的分组不展示；全部无结果时显示统一空状态。
- 仅在空查询时，在浮层底部显示“输入以搜索工具和文件”提示。
- MCP 工具展示为“服务名 / 工具名”，次级文本为工具描述。
- 文件展示文件名，次级文本为 `media/...` 或 `static/...` 相对路径，以区分同名文件。
- 输入框内将选中项渲染为带类型图标的不可编辑 token：
  - 技能：`@技能名`
  - MCP：`@服务名/工具名`
  - 文件：`@文件名`
- 支持上下键、Enter、Escape 和 Backspace 等现有键盘操作。

## @ 浮层 UI 优化

- 缩小浮层中每个条目的最小高度、内边距和字体，降低整体高度。
- 所有条目内容左对齐，统一图标、标题和描述的布局起点。
- 描述、MCP 工具说明和文件路径均限制单行并省略截断。
- 不提供 Tooltip。
- 保证浮层与条目不会产生横向滚动条；需要查看更多结果时仅纵向滚动。
- 保留分组标题、选中态、底部提示和键盘导航。

## 后端发现能力

- 扩展现有技能发现能力。
- 新增“可调用 MCP 工具”发现接口：
  - 仅返回启用且成功发现的工具。
  - MCP 服务失败或超时静默忽略，不展示错误行。
- 新增或扩展文件检索接口：
  - 仅搜索当前工作区的 `media` 与 `static`。
  - 返回文件名、根目录前缀相对路径、稳定标识和必要展示信息。
- 扩展聊天请求契约，传递技能、MCP 工具和文件三类结构化引用。
- 最终提交时重新验证技能、MCP 工具与文件引用有效性。

## 内存缓存

- 新增进程内“上下文引用目录缓存”。
- 缓存按“有效租户 + Agent”隔离，不跨租户、Agent、工作区或进程共享。
- 仅缓存发现结果和文件名索引，不缓存文件内容。
- 仅在用户输入 `@` 打开浮层时按需构建。
- TTL 固定为：
  - 技能：5 分钟
  - MCP 工具：3 分钟
  - 文件名索引：3 分钟
- 本期仅依赖 TTL 自动过期；不处理配置修改、上传或文件变化的即时失效。
- 硬过期：缓存到期刷新失败时，不返回旧快照；失败类别视为无结果。
- 最多保留 128 个缓存条目，采用 LRU 淘汰，并在访问时清理过期条目。
- 对同一“租户 + Agent + 资源类型”使用 single-flight 刷新，避免并发重复扫描或连接。
- TTL、容量和超时均为后端固定常量，不新增配置页面、环境变量或 API 配置。

## 性能边界

- MCP 工具发现并行执行：
  - 单个服务超时 2 秒。
  - 整体最多等待 3 秒。
  - 只展示在预算内成功发现的工具。
- 文件索引限制：
  - `media` 最多索引最近修改的 5,000 个文件。
  - `static` 最多索引最近修改的 5,000 个文件。

## 测试与验收

- 覆盖空 `@`、搜索、分组顺序、空状态和底部提示。
- 覆盖紧凑布局、左对齐、单行截断与无横向滚动。
- 覆盖三类 token 的展示、删除、去重与发送后清空。
- 覆盖 MCP 成功、失败、超时、同名工具和静默降级。
- 覆盖文件名搜索、`media`/`static` 同名路径区分、索引容量与路径校验。
- 覆盖缓存命中、TTL 到期、硬过期、LRU、single-flight 和租户/Agent 隔离。
