{
  "enabled": true,
  "events": {
    "BeforeStop": [
      {
        "id": "completion-gate",
        "hooks": [
          {
            "id": "before-stop-prompt-judge",
            "type": "prompt",
            "timeout": 8,
            "failPolicy": "block",
            "prompt": "请只基于 HookContext JSON 中可见的 assistant_response、prompt、tool_name、tool_response 和 error 判断当前任务是否允许结束。若 assistant_response 声称“已完成测试 / 已完成构建 / 已验证输出齐全”，但在当前上下文里没有任何可见依据，则返回 block，并明确指出还缺少哪类验证；若当前回复已经如实说明完成项与未完成项，则返回 allow。"
          }
        ]
      }
    ]
  }
}
